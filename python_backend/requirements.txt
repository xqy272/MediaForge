# MediaForge Python Backend Dependencies

# AI Background Removal (direct ONNX Runtime inference, rembg no longer required)
numpy>=1.24.0

# Image Processing
Pillow>=10.0.0

# Video Processing (headless: no Qt GUI dependency, avoids DLL issues in bundled env)
opencv-python-headless>=4.8.0

# GPU Acceleration (choose one based on hardware)
# For NVIDIA GPU:
# onnxruntime-gpu>=1.15.0

# For AMD/Intel GPU (Windows):
# onnxruntime-directml>=1.15.0

# For CPU only:
onnxruntime>=1.15.0
